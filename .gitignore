# Secrets - NEVER commit these
.env
.env.local
.env.*.local
server/.env

# Dependencies
node_modules/
server/node_modules/

# OS / editor
.DS_Store
*.log
npm-debug.log*

# Data (audit ledger / mandate state are runtime data, not source)
data/*.json
!data/.gitkeep
